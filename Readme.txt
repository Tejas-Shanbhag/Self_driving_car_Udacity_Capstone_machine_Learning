1:run the vells in self driving car.ipynb file.

2: you can load the model weights insted of training the network all over again.

3:after the the last cell containing opencv2 module you will have two popup windows visible in  the taskbar.One contains the road image and the other contains predicted steering angle